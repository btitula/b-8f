# Example environment configuration
# All values below are placeholders only — do not use real credentials.
# pragma: allowlist secret MONGODB_DATABASE="__EXAMPLE_DATABASE__"
# pragma: allowlist secret MONGODB_URI="mongodb://__USER__:__PASSWORD__@localhost:27017/?retryWrites=true&w=majority"
# pragma: allowlist secret PORT=3000